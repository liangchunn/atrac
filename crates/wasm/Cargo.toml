[package]
name = "atrac-wasm"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
at3 = { path = "../at3" }
at3p = { path = "../at3p" }
wasm-bindgen = "0.2"
